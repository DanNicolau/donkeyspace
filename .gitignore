/target/
/node_modules/
/web/node_modules/
/web/dist/
*.tsbuildinfo
.env
*.log

# Deployment-owned credentials are written here only when an explicit local
# config directory points into the source tree. Compose defaults use the
# checked-in non-secret placeholder instead.
.donkeyspace/secrets/
